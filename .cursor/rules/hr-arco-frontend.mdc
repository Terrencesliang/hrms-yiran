---
description: HR 模块前端统一使用 Arco Design，并按页面/公共组件拆分
globs: apps/employee_roster/org_ui/**,apps/employee_roster/employee_roster/public/**,apps/employee_roster/employee_roster/hr_roster/page/**,apps/employee_roster/employee_roster/hr_roster/sidebar/**
alwaysApply: false
---

# HR 前端：Arco Design + 组件化

HR 新页面和改造一律走 `apps/employee_roster/org_ui`（Vue 3 + `@arco-design/web-vue`）。Frappe Desk 的 `page/*.js` 只负责挂载，不写整页 UI。

## 组件优先

- 按钮、表格、表单、抽屉、弹窗、菜单、分页、空状态、反馈：直接用 Arco 官方组件（`a-button`、`a-table`、`a-form`、`a-drawer`、`a-modal`、`a-menu`、`a-empty`、`Message`）。
- 不要手写同类控件，不要再为新 HR 页面堆 jQuery / 原生 HTML 表单。
- 图标用 `@arco-design/web-vue/es/icon`，不要 emoji 当图标。

```vue
<!-- ❌ -->
<button class="hr-btn" @click="save">保存</button>

<!-- ✅ -->
<a-button type="primary" :loading="saving" @click="save">保存</a-button>
```

## 目录与复用

```
org_ui/src/
  pages/<feature>/     页面编排，只拼组件、接数据
  components/          跨页面公共组件（先搜这里再新建）
  composables/         跨页面逻辑
  api/                 frappe.call 与业务 API
```

- 单个 `.vue` 超过约 250 行或承担多种职责时，拆成页面子组件或 composable。
- 新增功能先复用 `components/`、`composables/`；确认没有同类实现再新增。
- 颜色、间距、主色沿用 `styles.css` 的 HR token（主色 `#00b386` / `--arcoblue-*`），不要页面内散落新色板。

## 改造边界

- 只改当前需求涉及的页面；旧 jQuery HR 页按需迁移，不做无关重写。
- 拆分和抽取不得改变接口、路由和业务结果。
- 改完 `org_ui/src` 后执行 `npm run build`，让 `public/org_ui/` 产物同步。
